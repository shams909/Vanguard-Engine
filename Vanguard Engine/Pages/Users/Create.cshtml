@page
@model Vanguard_Engine.Pages.Users.CreateModel
@{
    ViewData["Title"] = "Create User";
}

<h2>Create User</h2>

<form method="post" class="mt-3">
    <div class="mb-3">
        <label class="form-label">Username</label>
        <input class="form-control" asp-for="User.Username" />
    </div>
    <div class="mb-3">
        <label class="form-label">Email</label>
        <input class="form-control" asp-for="User.Email" />
    </div>
    <div class="mb-3">
        <label class="form-label">Password</label>
        <input class="form-control" asp-for="User.Password" type="password" />
    </div>
    <div class="mb-3">
        <label class="form-label">Address</label>
        <input class="form-control" asp-for="User.Address" />
    </div>
    <div class="mb-3">
        <label class="form-label">Role</label>
        <select class="form-select" asp-for="User.RoleId">
            <option value="">Select role</option>
            @foreach (var role in Model.Roles)
            {
                <option value="@role.Id">@role.RoleName</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label class="form-label">Last Login</label>
        <input class="form-control" asp-for="User.LastLogin" type="datetime-local" />
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
    <a class="btn btn-secondary" asp-page="/Users/Index">Cancel</a>
</form>
